[package]
name = "spidj-engine"
version = "0.0.1"
edition.workspace = true
description = "Pure suggestion engine for spidj. Rust port of apps/spidj/src/engine/."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
